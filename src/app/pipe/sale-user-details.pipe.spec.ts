import { SaleUserDetailsPipe } from './sale-user-details.pipe';

describe('SaleUserDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new SaleUserDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
